import type {Metadata} from "next";import {notFound} from "next/navigation";import {isLocale} from "@/lib/i18n";import {alternates} from "@/lib/seo";import {StaticPage} from "@/components/StaticPage";import {aboutCopy,staticCopy} from "@/lib/site-copy";export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};const copy=staticCopy[locale];return{title:{absolute:`${copy.termsTitle} | BZMAGNET`},description:copy.supportIntro,alternates:alternates(locale,"terms")}}export default async function Terms({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const copy=staticCopy[locale];return <StaticPage eyebrow="BZMAGNET" title={copy.termsTitle} intro={copy.supportIntro}><p>{aboutCopy[locale].termsText}</p></StaticPage>}
