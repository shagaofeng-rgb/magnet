import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const model=fs.readFileSync("lib/editorial.ts","utf8"),workflow=fs.readFileSync("lib/editorial-workflow.ts","utf8"),renderer=fs.readFileSync("components/ArticleRenderer.tsx","utf8");test("structured renderer avoids raw markdown and html",()=>{assert.doesNotMatch(renderer,/dangerouslySetInnerHTML|react-markdown/);for(const type of ["paragraph","bullets","numbered","checklist","callout","table","image"])assert.ok(model.includes(type))});test("quality gates cover required defects",()=>{for(const token of ["Update Note","Original Source","sourceClaims","heading-count","company-evidence","locale-review","duplicate-title"])assert.ok(model.includes(token))});test("scheduler cadence and cap are configured",()=>{const v=JSON.parse(fs.readFileSync("vercel.json","utf8"));assert.equal(v.crons.filter(job=>job.path.startsWith("/api/cron/news-")).length,2);assert.match(workflow,/slice\(0,1\)/)});test("public renderer does not expose internal editorial fields",()=>{assert.doesNotMatch(renderer,/topicBriefId|evidenceIds|generationRecordId|validationOutput|relevanceNote/)})
