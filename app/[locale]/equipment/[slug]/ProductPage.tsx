import type {Metadata} from "next";import {notFound} from "next/navigation";import {isLocale,origin} from "@/lib/i18n";import {findProduct,productPath} from "@/lib/product-model";import {ProductDetail} from "@/components/ProductDetail";import {alternates} from "@/lib/seo";export async function metadataFor({params}:{params:Promise<{locale:string;slug:string}>}):Promise<Metadata>{const{locale,slug}=await params;if(!isLocale(locale))return{};const p=findProduct(locale,slug);if(!p)return{};const l=p.locale[locale],path=productPath(locale,l.slug).split(`/${locale}/`)[1];return{title:l.metaTitle,description:l.metaDescription,alternates:alternates(locale,path),openGraph:{title:l.metaTitle,description:l.metaDescription,url:`${origin}${productPath(locale,l.slug)}`}}}export async function ProductPage({params}:{params:Promise<{locale:string;slug:string}>}){const{locale,slug}=await params;if(!isLocale(locale))notFound();const p=findProduct(locale,slug);if(!p)notFound();return <ProductDetail product={p} locale={locale}/>}
