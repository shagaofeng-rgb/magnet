import Link from "next/link";import { copy } from "@/lib/content";import { Locale,localeNames,localePath,locales } from "@/lib/i18n";
const paths=["equipment","industries","solutions","resources","about","contact"];
export function SiteHeader({locale}:{locale:Locale}){const c=copy[locale];return <><a className="skip" href="#content">Skip to content</a><div className="topline"/><header><div className="shell header-row"><Link className="brand" href={localePath(locale)}><span className="brand-mark">B</span>BZMAGNET</Link><nav aria-label="Primary">{c.nav.map((x,i)=><Link key={paths[i]} href={localePath(locale,paths[i])}>{x}</Link>)}<Link href={localePath(locale,"request-quote")}>{c.quote}</Link></nav><details className="locale"><summary>{localeNames[locale]}</summary>{locales.map(l=><div key={l}><Link href={localePath(l)}>{localeNames[l]}</Link></div>)}</details></div></header></>}
