import Link from "next/link";import {StaticPage} from "@/components/StaticPage";import {isLocale,localePath} from "@/lib/i18n";import {pageTitle} from "@/lib/page-copy";import {notFound} from "next/navigation";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();return <StaticPage eyebrow="Contact" title={pageTitle("contact",locale)}><p>For an efficient first review, provide material, particle size, throughput, target metal, installation dimensions, environment and project objective.</p><Link className="btn btn-primary" href={localePath(locale,"request-quote")}>Send process details</Link></StaticPage>}
