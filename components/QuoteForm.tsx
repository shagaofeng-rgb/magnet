"use client";import {useState} from "react";export function QuoteForm(){const[state,setState]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState("Sending...");const form=new FormData(e.currentTarget);const body=Object.fromEntries(form);const response=await fetch("/api/inquiries",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});setState(response.ok?"Received. We will review your process details.":"Unable to send. Please check the required fields and service configuration.")}return <form className="quote-form" onSubmit={submit}><label className="field">Name<input name="name" required maxLength={80}/></label><label className="field">Work email<input name="email" type="email" required maxLength={160}/></label><label className="field">Company<input name="company" required maxLength={120}/></label><label className="field">Industry<input name="industry" maxLength={100}/></label><label className="field full">Material and particle size<textarea name="material" required minLength={10} maxLength={1500}/></label><label className="field full">Throughput, dimensions, environment and objective<textarea name="process" required minLength={10} maxLength={2500}/></label><label className="field full consent"><input name="consent" type="checkbox" required/> I consent to BZMAGNET using this information to respond to my enquiry.</label><input name="website" tabIndex={-1} autoComplete="off" className="honeypot" aria-hidden="true"/><div className="field full"><button className="btn btn-primary" type="submit">Send process details</button><output aria-live="polite">{state}</output></div></form>}
