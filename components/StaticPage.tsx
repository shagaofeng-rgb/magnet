import {PageHero} from "./PageHero";export function StaticPage({eyebrow,title,intro,children}:{eyebrow:string;title:string;intro:string;children:React.ReactNode}){return <><PageHero eyebrow={eyebrow} title={title} intro={intro}/><section className="section section-white"><div className="shell prose">{children}</div></section></>}
