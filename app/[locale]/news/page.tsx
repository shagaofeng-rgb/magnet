import {PageHero} from "@/components/PageHero";import {isLocale} from "@/lib/i18n";import {newsCopy} from "@/lib/navigation";import {notFound} from "next/navigation";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const c=newsCopy[locale];return <><PageHero eyebrow="News" title={c.newsTitle} intro={c.newsIntro}/><section className="section"><div className="shell empty-state"><span className="product-code">EDITORIAL STATUS</span><h2>No public news items yet</h2><p>BZMAGNET will publish only verified company and catalog updates. Empty is better than unverified filler.</p></div></section></>}
