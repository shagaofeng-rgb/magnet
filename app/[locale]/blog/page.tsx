import Image from "next/image";import Link from "next/link";import {notFound} from "next/navigation";import {isLocale,localePath} from "@/lib/i18n";import {articlePath,editorialAssets,publishedArticles} from "@/lib/editorial";export default async function Blog({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const items=publishedArticles.filter(x=>x.locale===locale&&x.contentType==="blog"),featured=items[0];return <><section className="editorial-hero"><div className="shell"><span className="eyebrow">Buyer education</span><h1>Practical Guides for Magnetic Separation Equipment</h1><p>Structured guidance for preparing process information and reviewing equipment options.</p></div></section><div className="shell topic-grid">{["Choosing Equipment","Conveyor Applications","Bulk Materials","Recycling & Sorting","Requesting a Quote"].map(x=><div key={x}>{x}</div>)}</div><section className="editorial-section"><div className="shell">{featured?<Link className="featured-guide" href={articlePath(featured)}><Image src={editorialAssets.guide.src} alt={editorialAssets.guide.alt} width={768} height={512}/><div><span>BLOG</span><h2>{featured.title}</h2><p>{featured.summary}</p><strong>Read Article →</strong></div></Link>:<div className="empty-state">No approved guides are available in this locale yet.</div>}<div className="inline-cta"><span>Start with your process details.</span><Link href={localePath(locale,"request-quote")}>Request a Quote</Link></div></div></section></>}
