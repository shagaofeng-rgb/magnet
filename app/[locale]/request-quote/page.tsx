import type {Metadata} from "next";import {PageHero} from "@/components/PageHero";import {QuoteForm} from "@/components/QuoteForm";import {isLocale} from "@/lib/i18n";import {alternates} from "@/lib/seo";import {quoteCopy} from "@/lib/site-copy";import {notFound} from "next/navigation";export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};const copy=quoteCopy[locale];return{title:{absolute:`${copy.title} | BZMAGNET`},description:copy.intro,alternates:alternates(locale,"request-quote")}}export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const copy=quoteCopy[locale];return <><PageHero eyebrow={copy.eyebrow} title={copy.title} intro={copy.intro}/><section className="section"><div className="shell"><QuoteForm locale={locale}/></div></section></>}
