import Link from "next/link";import {PageHero} from "@/components/PageHero";import {isLocale,localePath} from "@/lib/i18n";import {industryNavigation} from "@/lib/navigation";import {homeCopy} from "@/lib/site-copy";import {notFound} from "next/navigation";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const copy=homeCopy[locale];return <><PageHero eyebrow={copy.industryTitle} title={copy.industryTitle} intro={copy.industryDescription}/><section className="section"><div className="shell grid">{industryNavigation.map((item,index)=><article className="card" key={item.slug}><span className="product-code">0{index+1}</span><h2>{copy.industries[index].title}</h2><p>{copy.industryDescription}</p><Link className="card-link" href={localePath(locale,`industry-solutions/${item.slug}`)}>{copy.exploreSolution} →</Link></article>)}</div></section></>}
