import {StaticPage} from "@/components/StaticPage";import {aboutCopy,staticCopy} from "@/lib/site-copy";import {isLocale} from "@/lib/i18n";import {notFound} from "next/navigation";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const copy=staticCopy[locale];return <StaticPage eyebrow={copy.privacyEyebrow} title={copy.privacyTitle} intro={copy.supportIntro}><p>{aboutCopy[locale].privacyText}</p></StaticPage>}
