import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";
test("catalog source has five locale records and unique UUIDs",()=>{const catalog=JSON.parse(fs.readFileSync("data/products.generated.json","utf8"));assert.equal(catalog.totalSource,88);assert.equal(new Set(catalog.products.map(p=>p.id)).size,catalog.products.length);for(const product of catalog.products)for(const locale of ["en","es","pt","ar","ru"]){assert.ok(product.locale[locale]?.title);assert.ok(product.locale[locale]?.slug)}});
test("private migration maps remain excluded from public paths",()=>{assert.equal(fs.existsSync("data/migrations/cowin-to-bzmagnet-map.private.json"),false);assert.ok(fs.readFileSync(".gitignore","utf8").includes("data/private-migration/"))});
