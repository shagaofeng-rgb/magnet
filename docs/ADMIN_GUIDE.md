# Admin guide

The admin information architecture covers catalog records, multilingual completion, structured publishing, media ownership, SEO controls, enquiries, roles, audit history and guarded imports. Before enabling writes, configure a private data store, role records, `ADMIN_SESSION_SECRET`, an administrator password hash, immutable audit events and backup/rollback retention. Never store source-system URLs in public fields.
