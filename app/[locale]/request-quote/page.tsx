import {PageHero} from "@/components/PageHero";import {QuoteForm} from "@/components/QuoteForm";import {isLocale} from "@/lib/i18n";import {pageTitle} from "@/lib/page-copy";import {notFound} from "next/navigation";export const metadata={robots:{index:false,follow:true}};export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();return <><PageHero eyebrow="Request a quote" title={pageTitle("quote",locale)} intro="The more process detail you provide, the more useful the first equipment review can be."/><section className="section"><div className="shell"><QuoteForm/></div></section></>}
