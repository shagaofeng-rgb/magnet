# SEO and hreflang report

The implementation uses locale-prefixed routes, a single root redirect, dynamic sitemap, BZMAGNET-only origin, canonical alternates and `x-default`. Search and quote routes are noindex. Product JSON-LD renders only visible name, description, URL and BZMAGNET brand data. A built-output crawl remains required before deployment.
