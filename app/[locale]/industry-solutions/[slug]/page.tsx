import {notFound} from "next/navigation";import {ContextDetail} from "@/components/ContextDetail";import {isLocale,locales} from "@/lib/i18n";import {industryNavigation} from "@/lib/navigation";export function generateStaticParams(){return locales.flatMap(locale=>industryNavigation.map(({slug})=>({locale,slug})))}export default async function Page({params}:{params:Promise<{locale:string;slug:string}>}){const{locale,slug}=await params;if(!isLocale(locale)||!industryNavigation.some(x=>x.slug===slug))notFound();return <ContextDetail locale={locale} slug={slug} type="industry"/>}
