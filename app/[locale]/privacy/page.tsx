import type {Metadata} from "next";import {StaticPage} from "@/components/StaticPage";import {aboutCopy,staticCopy} from "@/lib/site-copy";import {isLocale} from "@/lib/i18n";import {alternates} from "@/lib/seo";import {notFound} from "next/navigation";export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};const copy=staticCopy[locale];return{title:{absolute:`${copy.privacyTitle} | BZMAGNET`},description:copy.supportIntro,alternates:alternates(locale,"privacy")}}export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const copy=staticCopy[locale];return <StaticPage eyebrow={copy.privacyEyebrow} title={copy.privacyTitle} intro={copy.supportIntro}><p>{aboutCopy[locale].privacyText}</p></StaticPage>}
