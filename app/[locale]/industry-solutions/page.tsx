import Link from "next/link";import {PageHero} from "@/components/PageHero";import {isLocale,localePath} from "@/lib/i18n";import {industryNavigation} from "@/lib/navigation";import {notFound} from "next/navigation";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();return <><PageHero eyebrow="Applications" title="Industry Solutions" intro="Start with the process environment and separation objective before choosing an equipment family."/><section className="section"><div className="shell grid">{industryNavigation.map((item,index)=><article className="card" key={item.slug}><span className="product-code">0{index+1}</span><h2>{item.title}</h2><p>Define material, throughput, target metal and installation constraints for this industry.</p><Link className="card-link" href={localePath(locale,`industry-solutions/${item.slug}`)}>Open solution →</Link></article>)}</div></section></>}
