import Link from "next/link";import {families,products} from "@/lib/content";const modules=["Products, series & models","Parameters & options","Multilingual completion","Guides & publishing","Media ownership","SEO & redirects","Enquiry inbox","Roles & audit log","Content history & rollback","Approved-fact import"];
export default function Admin(){return <div className="admin-grid"><aside><div className="brand">BZMAGNET</div><p>Content operations</p>{modules.map(x=><p key={x}>{x}</p>)}</aside><main><span className="eyebrow">Configuration-safe preview</span><h1>Catalog operations</h1><div className="grid"><article className="card"><span className="status">reviewed</span><h2>{products.length} products</h2><p>{families.length} active equipment families.</p></article><article className="card"><span className="status">5 locales</span><h2>Translation status</h2><p>All current product names and slugs are populated; long-form human review remains a release gate.</p></article><article className="card"><span className="status">guarded</span><h2>Import boundary</h2><p>Source domains, source links, old IDs, image URLs and tracking identifiers fail validation.</p></article></div><section className="section"><h2>Security state</h2><p className="notice">This repository ships the admin information architecture and server-side session verifier. Configure the private database, password hash, session secret and role store before enabling mutations.</p><Link href="/en/" className="btn btn-primary">View public site</Link></section></main></div>}
