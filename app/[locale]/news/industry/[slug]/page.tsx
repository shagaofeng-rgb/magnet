import {notFound} from "next/navigation";import {isLocale} from "@/lib/i18n";import {findArticle} from "@/lib/editorial";import {ArticleRenderer} from "@/components/ArticleRenderer";export default async function Page({params}:{params:Promise<{locale:string;slug:string}>}){const{locale,slug}=await params;if(!isLocale(locale))notFound();const a=findArticle(locale,"industry_news",slug);if(!a)notFound();return <ArticleRenderer article={a}/>}
