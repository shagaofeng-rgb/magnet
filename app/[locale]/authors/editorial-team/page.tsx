import type {Metadata} from "next";import {StaticPage} from "@/components/StaticPage";import {isLocale} from "@/lib/i18n";import {alternates} from "@/lib/seo";import {uiCopy} from "@/lib/ui-copy";import {notFound} from "next/navigation";export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};const copy=uiCopy[locale].author;return{title:{absolute:`${copy.title} | BZMAGNET`},description:copy.intro,alternates:alternates(locale,"authors/editorial-team")}}export default async function Author({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const copy=uiCopy[locale].author;return <StaticPage eyebrow={copy.eyebrow} title={copy.title} intro={copy.intro}><p>{copy.body}</p></StaticPage>}
