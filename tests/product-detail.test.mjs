import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const model=fs.readFileSync("lib/product-model.ts","utf8"),page=fs.readFileSync("components/ProductDetail.tsx","utf8");test("localized equipment route segments exist",()=>{for(const segment of ["equipment","equipos","equipamentos","المعدات","oborudovanie"])assert.ok(model.includes(segment))});test("publication validation blocks unsafe data",()=>{for(const token of ["product-image-approval","unverified-","english-required","magnet-conflict"])assert.ok(model.includes(token))});test("public page never exposes source ids",()=>{assert.doesNotMatch(page,/sourceId|approvedAt|verifiedName/)});test("field visibility degrades safely",()=>{assert.match(model,/visibility!=="hidden"/);assert.match(model,/Available on request/)});test("detail page has one h1 and supported schemas",()=>{assert.equal((page.match(/<h1/g)||[]).length,1);for(const type of ["Product","BreadcrumbList","FAQPage"])assert.ok(page.includes(type))});
