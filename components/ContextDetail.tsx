import Link from "next/link";import {Locale,localePath} from "@/lib/i18n";import {itemNames} from "@/lib/page-copy";import {PageHero} from "./PageHero";export function ContextDetail({locale,slug,type}:{locale:Locale;slug:string;type:"industry"|"solution"}){const name=itemNames[slug]?.[locale]||slug.split("-").map(word=>word.charAt(0).toUpperCase()+word.slice(1)).join(" ");return <><PageHero eyebrow={type} title={name} intro="Build the selection around verified process conditions and the required separation objective."/><section className="section"><div className="shell detail-grid"><div><h2>Define the duty</h2><p>Confirm the feed material, particle-size range, target metal or contaminant, nominal and peak throughput, and the desired downstream result.</p><h2>Check the installation</h2><p>Provide conveyor, chute or pipe dimensions, speed, available space, environment, electrical requirements and cleaning constraints.</p><h2>Validate before purchase</h2><p>Final suitability depends on representative material data and an agreed configuration. Unknown parameters remain unpublished until approved.</p></div><aside className="card"><h2>Prepare a useful enquiry</h2><p>Send photos, a process sketch and operating data where available.</p><Link className="btn btn-primary" href={localePath(locale,"request-quote")}>Send process details</Link></aside></div></section></>}
