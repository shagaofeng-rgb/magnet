import {StaticPage} from "@/components/StaticPage";import {isLocale} from "@/lib/i18n";import {pageTitle} from "@/lib/page-copy";import {notFound} from "next/navigation";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();return <StaticPage eyebrow="About" title={pageTitle("about",locale)}><h2>Independent brand, clear scope</h2><p>BZMAGNET organizes equipment information around buyer inputs and publishes only reviewed facts. We do not claim facilities, certificates, stock or performance that have not been approved for this brand.</p><h2>Editorial controls</h2><p>Technical values remain hidden until a source and reviewer approve them. Localized content has an explicit review state.</p></StaticPage>}
