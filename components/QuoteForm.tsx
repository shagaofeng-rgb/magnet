"use client";import {useState} from "react";
export function QuoteForm(){const[state,setState]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState("Sending…");const form=new FormData(e.currentTarget);const body=Object.fromEntries(form);const r=await fetch("/api/inquiries",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});setState(r.ok?"Received. We will review your process details.":"Unable to send. Please check the required fields.")}return <form className="quote-form" onSubmit={submit}><label className="field">Name<input name="name" required maxLength={80}/></label><label className="field">Work email<input name="email" type="email" required maxLength={160}/></label><label className="field">Company<input name="company" required maxLength={120}/></label><label className="field">Industry<input name="industry" maxLength={100}/></label><label className="field full">Material and particle size<textarea name="material" required maxLength={1500}/></label><label className="field full">Throughput, dimensions, environment and objective<textarea name="process" required maxLength={2500}/></label><label className="field full"><span><input name="consent" type="checkbox" required/> I consent to BZMAGNET using this information to respond to my enquiry.</span></label><input name="website" tabIndex={-1} autoComplete="off" style={{position:"absolute",left:"-9999px"}}/><div className="field full"><button className="btn btn-primary" type="submit">Send process details</button><output aria-live="polite">{state}</output></div></form>}
