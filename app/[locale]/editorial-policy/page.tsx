import type {Metadata} from "next";import {StaticPage} from "@/components/StaticPage";import {isLocale} from "@/lib/i18n";import {alternates} from "@/lib/seo";import {uiCopy} from "@/lib/ui-copy";import {notFound} from "next/navigation";export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};const copy=uiCopy[locale].policy;return{title:{absolute:`${copy.title} | BZMAGNET`},description:copy.intro,alternates:alternates(locale,"editorial-policy")}}export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const copy=uiCopy[locale].policy;return <StaticPage eyebrow={copy.eyebrow} title={copy.title} intro={copy.intro}>{copy.paragraphs.map(paragraph=><p key={paragraph}>{paragraph}</p>)}</StaticPage>}
