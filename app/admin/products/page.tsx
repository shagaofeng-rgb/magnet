import {productRecords,validateProductForPublication} from "@/lib/product-model";export default function ProductAdmin(){return <main><span className="eyebrow">Private product editor</span><h1>Product publication controls</h1><p className="notice">Mutations remain disabled until the private database, authenticated roles and audit store are configured.</p>{productRecords.map(p=><article className="card" key={p.id}><span className="status">{p.status}</span><h2>{p.verifiedName}</h2><p>Validation: {validateProductForPublication(p).length?validateProductForPublication(p).join(", "):"ready"}</p><ul><li>Verified sources, conditions and approval dates</li><li>Public / on-request / hidden visibility</li><li>Product media approval and alt text</li><li>Five-locale review and RTL preview</li><li>FAQ, related products, SEO and redirect history</li><li>Audit log and rollback revisions</li></ul></article>)}</main>}
