import Link from "next/link";import {Locale,localePath} from "@/lib/i18n";import {itemNames} from "@/lib/page-copy";import {uiCopy} from "@/lib/ui-copy";import {PageHero} from "./PageHero";export function ContextDetail({locale,slug,type}:{locale:Locale;slug:string;type:"industry"|"solution"}){const name=itemNames[slug]?.[locale]||slug.split("-").map(word=>word.charAt(0).toUpperCase()+word.slice(1)).join(" ");const copy=uiCopy[locale].context;return <><PageHero eyebrow={type==="industry"?copy.industry:copy.solution} title={name} intro={copy.intro}/><section className="section"><div className="shell detail-grid"><div><h2>{copy.dutyTitle}</h2><p>{copy.dutyText}</p><h2>{copy.installationTitle}</h2><p>{copy.installationText}</p><h2>{copy.validateTitle}</h2><p>{copy.validateText}</p></div><aside className="card"><h2>{copy.enquiryTitle}</h2><p>{copy.enquiryText}</p><Link className="btn btn-primary" href={localePath(locale,"request-quote")}>{copy.send}</Link></aside></div></section></>}
