import {ListingPage} from "@/components/ListingPage";import {solutions} from "@/lib/content";import {isLocale} from "@/lib/i18n";import {pageTitle} from "@/lib/page-copy";import {notFound} from "next/navigation";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();return <ListingPage locale={locale} title={pageTitle("solutions",locale)} eyebrow="Solutions" items={solutions} base="solutions"/>}
