import {articles,validateArticle} from "@/lib/editorial";export default function EditorialAdmin(){return <main><span className="eyebrow">Private editorial workflow</span><h1>News & Blog publishing</h1><p className="notice">Candidate review runs every 12 hours. The approved queue checks every 48 hours and publishes at most one validated item.</p>{articles.map(a=><article className="card" key={a.id}><span className="status">{a.status}</span><h2>{a.title}</h2><p>{validateArticle(a).length?validateArticle(a).join(", "):"All publication gates passed"}</p></article>)}</main>}
