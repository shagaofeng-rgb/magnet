import Link from "next/link";import {Locale,localePath} from "@/lib/i18n";import {itemNames} from "@/lib/page-copy";import {uiCopy} from "@/lib/ui-copy";import {PageHero} from "./PageHero";
export function ListingPage({locale,title,items,base}:{locale:Locale;title:string;items:string[];base:string}){const copy=uiCopy[locale].listing;const eyebrow=base==="solutions"?copy.solution:copy.industry;return <><PageHero eyebrow={eyebrow} title={title} intro={copy.intro}/><section className="section"><div className="shell grid">{items.map((x,i)=><article className="card" key={x}><span className="product-code">0{i+1}</span><h2>{itemNames[x]?.[locale]||x}</h2><p>{copy.description}</p><Link className="card-link" href={localePath(locale,`${base}/${x}`)}>{copy.open} →</Link></article>)}</div></section></>}
