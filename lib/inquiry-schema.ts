import { z } from "zod";
export const productCategories=["Conveyor Magnetic Separators","Mineral & Bulk Separation","Recycling & Metal Sorting","Process Magnets & Filters","Other / Not sure"] as const;
export const inquirySchema=z.object({fullName:z.string().trim().min(2,"Enter your full name.").max(80),companyName:z.string().trim().min(2,"Enter your company name.").max(120),country:z.string().trim().min(2,"Enter your country.").max(80),businessEmail:z.string().trim().email("Enter a valid business email.").max(160),productCategory:z.enum(productCategories),message:z.string().trim().min(10,"Add at least 10 characters about your enquiry.").max(2500),phone:z.string().trim().max(50).optional().or(z.literal("")),productModel:z.string().trim().max(120).optional().or(z.literal("")),application:z.string().trim().max(300).optional().or(z.literal("")),requiredQuantity:z.string().trim().max(80).optional().or(z.literal("")),technicalRequirement:z.string().trim().max(160).optional().or(z.literal("")),productUrl:z.string().url().max(500).optional().or(z.literal("")),formType:z.enum(["quick","product","rfq","contact"]),website:z.string().max(0).optional(),});
export type InquiryInput=z.infer<typeof inquirySchema>;
