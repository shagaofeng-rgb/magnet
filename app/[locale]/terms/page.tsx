import {notFound} from "next/navigation";import {isLocale} from "@/lib/i18n";import {StaticPage} from "@/components/StaticPage";import {aboutCopy,staticCopy} from "@/lib/site-copy";export default async function Terms({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const copy=staticCopy[locale];return <StaticPage eyebrow="BZMAGNET" title={copy.termsTitle} intro={copy.supportIntro}><p>{aboutCopy[locale].termsText}</p></StaticPage>}
