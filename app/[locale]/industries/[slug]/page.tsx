import {notFound} from "next/navigation";import {ContextDetail} from "@/components/ContextDetail";import {industries} from "@/lib/content";import {isLocale,locales} from "@/lib/i18n";export function generateStaticParams(){return locales.flatMap(locale=>industries.map(slug=>({locale,slug})))}export default async function Page({params}:{params:Promise<{locale:string;slug:string}>}){const{locale,slug}=await params;if(!isLocale(locale)||!industries.includes(slug))notFound();return <ContextDetail locale={locale} slug={slug} type="industry"/>}
