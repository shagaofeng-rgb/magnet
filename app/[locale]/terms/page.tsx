import {notFound} from "next/navigation";import {isLocale} from "@/lib/i18n";import {StaticPage} from "@/components/StaticPage";export default async function Terms({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();return <StaticPage eyebrow="BZMAGNET" title="Terms"><p>Website information is provided for general product-sourcing and enquiry purposes. Equipment suitability, configuration, commercial terms and supply details must be confirmed in a written quotation based on the application information provided.</p></StaticPage>}
