import Link from "next/link";import {Locale,localePath} from "@/lib/i18n";import {itemNames} from "@/lib/page-copy";import {PageHero} from "./PageHero";
export function ListingPage({locale,title,eyebrow,items,base}:{locale:Locale;title:string;eyebrow:string;items:string[];base:string}){return <><PageHero eyebrow={eyebrow} title={title} intro="Use the process context to narrow the equipment family before discussing a model."/><section className="section"><div className="shell grid">{items.map((x,i)=><article className="card" key={x}><span className="product-code">0{i+1}</span><h2>{itemNames[x]?.[locale]||x}</h2><p>Define material, target contaminant, throughput and installation constraints for this application.</p><Link className="card-link" href={localePath(locale,`${base}/${x}`)}>Open guidance →</Link></article>)}</div></section></>}
