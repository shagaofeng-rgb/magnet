import type {Metadata} from "next";import {notFound} from "next/navigation";import {isLocale} from "@/lib/i18n";import {findProduct,productPath} from "@/lib/product-model";import {ProductDetail} from "@/components/ProductDetail";import {pageMetadata} from "@/lib/page-metadata";import {siteConfig} from "@/lib/site";
export async function metadataFor({params}:{params:Promise<{locale:string;slug:string}>}):Promise<Metadata>{const{locale,slug}=await params;if(!isLocale(locale))return{};const p=findProduct(locale,slug);if(!p)return{};const l=p.locale[locale],path=productPath(locale,l.slug).split(`/${locale}/`)[1],primary=p.media.find(item=>item.type==="product"&&item.approved&&!item.aiGenerated);return pageMetadata({locale,path,title:l.metaTitle,description:l.metaDescription,image:primary?.src||siteConfig.defaultOgImage});}
export async function ProductPage({params}:{params:Promise<{locale:string;slug:string}>}){const{locale,slug}=await params;if(!isLocale(locale))notFound();const p=findProduct(locale,slug);if(!p)notFound();return <ProductDetail product={p} locale={locale}/>}
