import {ProductRecord,validateProductForPublication} from "./product-model";export type ProductAuditEvent={id:string;productId:string;action:"create"|"update"|"publish"|"rollback";actorId:string;createdAt:string;before?:ProductRecord;after?:ProductRecord};export type ProductAdminDraft={product:ProductRecord;redirectHistory:Array<{from:string;to:string;createdAt:string}>;auditLog:ProductAuditEvent[];revision:number};export function publishProduct(draft:ProductAdminDraft){const errors=validateProductForPublication(draft.product);return errors.length?{success:false as const,errors}:{success:true as const,product:draft.product,revision:draft.revision+1}}export function rollbackProduct(draft:ProductAdminDraft,eventId:string){const event=draft.auditLog.find(x=>x.id===eventId);return event?.before?{success:true as const,product:event.before,revision:draft.revision+1}:{success:false as const,error:"revision_not_found"}}
