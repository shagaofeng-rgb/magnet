import {PageHero} from "@/components/PageHero";import {isLocale} from "@/lib/i18n";import {pageTitle} from "@/lib/page-copy";import {notFound} from "next/navigation";const items=["Separator selection checklist","Conveyor data worksheet","Material-test preparation","Maintenance planning questions"];export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();return <><PageHero eyebrow="Resources" title={pageTitle("resources",locale)} intro="Procurement notes written for practical technical comparison."/><section className="section"><div className="shell grid">{items.map((x,i)=><article className="card" key={x}><span className="product-code">GUIDE 0{i+1}</span><h2>{x}</h2><p>Structured guidance with visible facts, review status and no placeholder sections.</p></article>)}</div></section></>}
